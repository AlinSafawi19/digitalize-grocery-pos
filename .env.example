# DigitalizePOS - Environment Variables
# Copy this file to .env and update the values as needed

# =============================================================================
# Environment Configuration
# =============================================================================

# Node environment
# Optional - Usually set automatically by build tools (Vite/Electron)
# Values: development, production, test
# Development: Usually "development" (set by npm run electron:dev)
# Production: Usually "production" (set by build process)
# Note: You typically don't need to set this manually
# NODE_ENV=development

# =============================================================================
# Application Configuration
# =============================================================================

# Application name (used in UI and logs)
# Optional - Default: "DigitalizePOS"
APP_NAME=digitalize-grocery-pos

# Application version
# Optional - Default: "1.0.0"
APP_VERSION=1.0.0

# =============================================================================
# License Server Configuration
# =============================================================================

# License server API URL
# REQUIRED - No default value
# Development: http://localhost:3000
# Production: https://license.digitalizepos.com/api
LICENSE_SERVER_URL=http://localhost:3000

# =============================================================================
# Security Configuration
# =============================================================================

# Application secret key for encryption and security
# REQUIRED - No default value
# IMPORTANT: Generate a secure secret using: npm run generate:secret
# Development: Use generated secret from script
# Production: MUST use a strong, unique secret (never commit to git!)
# Example: APP_SECRET=d69022eb9e2e8f7c2a2a94f57e557efe6c02ce686ac97d54fc1adcc51bbdf362
APP_SECRET=42f69f8318f3db8c6ddc81cec1eb23f64490efdda99b638fe255a48eb7419a82

# =============================================================================
# Database Configuration
# =============================================================================

# Database file path (automatically set by the application)
# The database is stored at: {userData}/database/digitalizePOS.db
# You typically don't need to set this manually
# DATABASE_URL=file:/path/to/database/digitalizePOS.db
DATABASE_URL="file:./prisma/database/digitalizePOS.db"
# =============================================================================
# Development vs Production
# =============================================================================

# Development Setup:
# - LICENSE_SERVER_URL=http://localhost:3000 (REQUIRED)
# - APP_SECRET=<generate with: npm run generate:secret> (REQUIRED)
# - NODE_ENV is automatically set to "development" by npm run electron:dev
# - Other variables use defaults

# Production Setup:
# - LICENSE_SERVER_URL=https://license.digitalizepos.com/api (REQUIRED)
# - APP_SECRET=<strong-production-secret-generated-securely> (REQUIRED)
# - NODE_ENV is automatically set to "production" by build process
# - All sensitive values should be set via environment variables
# - Never commit .env file to version control

# =============================================================================
# Notes
# =============================================================================

# 1. REQUIRED variables (must be set):
#    - LICENSE_SERVER_URL
#    - APP_SECRET
#
# 2. Optional variables (have defaults):
#    - NODE_ENV (usually set automatically)
#    - APP_NAME
#    - APP_VERSION
#
# 3. Generate APP_SECRET for development:
#    npm run generate:secret
#
# 4. For production builds, set environment variables in your deployment
#    environment or use electron-builder's environment configuration
#
# 5. The .env file is git-ignored for security
#    Always use .env.example as a template
